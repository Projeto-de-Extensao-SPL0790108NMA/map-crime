# Aplicação / segurança
SECRET_KEY="&e56!_mf_)+s&-b)336fhgnw0ead%$)ifkba+x_5z#ug@lut#b"
# DEBUG: 0/1 ou true/false
DEBUG="1"

# Hosts e CSRF (sem espaços após vírgula)
ALLOWED_HOSTS="localhost,127.0.0.1,prdl.shop,api.prdl.shop,0.0.0.0"
CSRF_TRUSTED_ORIGINS="https://prdl.shop,https://api.prdl.shop,http://localhost:8000,https://0.0.0.0:8000"

# CORS
CORS_ALLOW_ALL=True
CORS_ALLOWED_ORIGINS="https://prdl.shop,https://api.prdl.shop,http://localhost:8000,https://0.0.0.0:8000"
# Localização
LANGUAGE_CODE="pt-br"
TIME_ZONE="America/Manaus"

# Banco de dados (Postgres)
DB_ENGINE="django.contrib.gis.db.backends.postgis"
POSTGRES_DB="mydatabase"
POSTGRES_USER="my_user"
POSTGRES_PASSWORD="my_password"
#POSTGRES_HOST="psql"        # rodando o make run-docker use esse
POSTGRES_HOST="127.0.0.1"     # rodando o make run-dev use esse
POSTGRES_PORT="5432"

# Google / Social
GOOGLE_CLIENT_ID="seu_client_id.apps.googleusercontent.com"
RECAPTCHA_SECRET_KEY="seu_recaptcha_secret_key"

# Email (SMTP)
DEFAULT_FROM_EMAIL="no-reply@example.com"
EMAIL_BACKEND="django.core.mail.backends.smtp.EmailBackend"
EMAIL_HOST="127.0.0.1"
EMAIL_PORT="1025"
EMAIL_HOST_USER=""
EMAIL_HOST_PASSWORD=""
EMAIL_USE_TLS=False
EMAIL_USE_SSL=False

# JWT / Auth
PROJECT_NAME="My App"
JWT_ACCESS_TOKEN_LIFETIME_DAYS=60
JWT_REFRESH_TOKEN_LIFETIME_DAYS=90
JWT_ROTATE_REFRESH_TOKENS=False
JWT_BLACKLIST_AFTER_ROTATION=True
JWT_AUTH_HEADER_TYPES="Bearer"

# APP / docs
APP_NAME="My App"
API_PUBLIC_URL="https://api.prdl.shop"

# Notas:
# - Não deixe espaços em volta do '=' e nem após vírgulas nas listas.
# - Em produção não comite esse arquivo com segredos.