# Copie este arquivo para:
# - `.env.dev`  → utilizado pelo `make run-dev`, `make run-test` e comandos locais.
# - `.env.prod` → utilizado pelo `make run-docker` (stack completa em containers).
#
# Ajuste os valores conforme cada ambiente. Em especial:
# - `.env.dev`: mantenha `DEBUG=1` e `POSTGRES_HOST=127.0.0.1`.
# - `.env.prod`: defina `DEBUG=0`, use segredos reais e troque `POSTGRES_HOST=psql`
#   (nome do serviço do banco definido em `docker-compose.yml`).

# Aplicação / segurança
SECRET_KEY="changeme"
DEBUG="1"

# Hosts e CSRF
ALLOWED_HOSTS="localhost,127.0.0.1,0.0.0.0"
CSRF_TRUSTED_ORIGINS="http://localhost:8000,http://0.0.0.0:8000"

# CORS
CORS_ALLOW_ALL=True
CORS_ALLOWED_ORIGINS="http://localhost:8000,http://0.0.0.0:8000"

# Localização
LANGUAGE_CODE="pt-br"
TIME_ZONE="America/Manaus"

# Banco de dados (Postgres)
DB_ENGINE="django.contrib.gis.db.backends.postgis"
POSTGRES_DB="mapcrime"
POSTGRES_USER="mapcrime"
POSTGRES_PASSWORD="mapcrime"
POSTGRES_PORT="5432"

# utiliza o "127.0.0.1" no `.env.dev`
#POSTGRES_HOST="127.0.0.1"

# utiliza o "psql" no `.env.prod`
#POSTGRES_HOST="psql"

# Google / Social
GOOGLE_CLIENT_ID="seu_client_id.apps.googleusercontent.com"
RECAPTCHA_SECRET_KEY="seu_recaptcha_secret_key"

# Email (SMTP)
DEFAULT_FROM_EMAIL="no-reply@example.com"
EMAIL_BACKEND="django.core.mail.backends.smtp.EmailBackend"
EMAIL_HOST="127.0.0.1"
EMAIL_PORT="1025"
EMAIL_HOST_USER=""
EMAIL_HOST_PASSWORD=""
EMAIL_USE_TLS=False
EMAIL_USE_SSL=False

# JWT / Auth
PROJECT_NAME="MapaCrime"
JWT_ACCESS_TOKEN_LIFETIME_DAYS=60
JWT_REFRESH_TOKEN_LIFETIME_DAYS=90
JWT_ROTATE_REFRESH_TOKENS=False
JWT_BLACKLIST_AFTER_ROTATION=True
JWT_AUTH_HEADER_TYPES="Bearer"

# APP / docs
APP_NAME="MapaCrime"
API_PUBLIC_URL="http://localhost:8000"

# Notas:
# - Nunca commite `.env.dev` ou `.env.prod`.
# - Ajuste credenciais reais em produção e mantenha `DEBUG=0`.
# - Respeite os formatos sem espaços ao redor de `=`.
